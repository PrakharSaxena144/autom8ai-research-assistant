# Runtime dependencies. Python 3.11 or 3.12 recommended.
# Everything here is open source; the only external services are free tiers (Groq, optional Tavily / Qdrant Cloud).

# API
fastapi>=0.115,<1
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7,<3
python-dotenv>=1.0
httpx>=0.27

# Agent + LLM (Groq free tier)
langchain-core>=1.0,<2
langchain-groq>=1.0,<2
langchain-text-splitters>=1.0,<2
langgraph>=1.0,<2

# Vector store + local embeddings (no API key)
langchain-qdrant>=1.0,<2
qdrant-client>=1.12
fastembed>=0.5

# Document parsing + OCR (OCR also needs the tesseract binary, see README)
pypdf>=5.0
pypdfium2>=4.30
pytesseract>=0.3.10
pillow>=10.0
python-docx>=1.1
beautifulsoup4>=4.12

# Web fallback without a key (Tavily is called over plain HTTP when TAVILY_API_KEY is set)
ddgs>=9.0
